% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SPQRX.R
\name{predict_spqrx}
\alias{predict_spqrx}
\title{Predict from SPQR / SPQRX Model}
\usage{
predict_spqrx(
  object,
  x,
  y = NULL,
  type = "QF",
  tau = 0.5,
  normalize_input = FALSE,
  normalize_output = TRUE
)
}
\arguments{
\item{object}{A fitted model object returned by \code{fit_spqrx()},
\code{fit.spqrx()}, or \code{fit.spqr()}.}

\item{x}{Matrix or data frame of covariates for prediction.}

\item{y}{Optional numeric vector of response values. Required for
\code{type = "CDF"} and \code{type = "PDF"}.}

\item{type}{Character string specifying prediction type:
\code{"QF"} (quantile function), \code{"CDF"}, or \code{"PDF"}.}

\item{tau}{Numeric value or vector of quantile levels in (0,1).
Used when \code{type = "QF"}.}

\item{normalize_input}{Logical. If FALSE (default), covariates and
response values are normalized using stored model parameters.}

\item{normalize_output}{Logical. If TRUE (default), quantile predictions
are transformed back to the original response scale.}
}
\value{
\itemize{
  \item For \code{type = "QF"}: Matrix of predicted quantiles.
  \item For \code{type = "CDF"}: Matrix or vector of cumulative probabilities.
  \item For \code{type = "PDF"}: Matrix or vector of density values.
}
}
\description{
Generates predictions from a fitted SPQR or SPQRX model object.
Supports cumulative distribution (CDF), probability density (PDF),
and quantile function (QF) evaluation.
}
